# Zervan v40 Candidate Surface

State: CANDIDATE ONLY

Authority: NONE

Human Gate: ACTIVE

Canonical Impact: NONE

External Runtime: DISABLED

System Population: DISALLOWED

This directory contains inert local engineering artifacts proposed for v40.

Nothing in this directory promotes v40, rewrites the v39 canonical body, changes authority, activates an external runtime, or populates a system. Promotion requires completed verification and an explicit Human Gate decision.

Current candidate controls:

- `governance/` freezes the failure identifiers, severity taxonomy, remediation controls, implementation waves, acceptance evidence, and closure state.
- `contracts/` defines the Operational Contract, runtime state, and transition event objects and binds the first tranche to current Git main.
- `tools/` validates the candidate registry and contract without third-party dependencies.
- `receipts/` records verification without claiming promotion or authority.
