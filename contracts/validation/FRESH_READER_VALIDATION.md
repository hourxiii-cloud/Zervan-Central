# R6-M — Fresh-Reader Validation

Status: CONTROLLED CANDIDATE VALIDATION CONTRACT
Ring: R6-M
Tranche: 8 — Validation and Audit
Validation Vector: FRESH_READER_TEST
Version: vTemporal.41.0
Implementation Identity: v41 Complete
Promotion State: CANDIDATE
Authority: NONE
Human Gate: ACTIVE

---

## 0. Purpose

R6-M implements the native-v41 Fresh-Reader Test.

Validation SHALL verify that a fresh reader of the native v41 repository can
understand and operate the system without requiring:

- v40 knowledge;
- prior conversation state;
- original architect interpretation;
- emotional discovery history.

A repository that requires an external decoder ring is incomplete.

Fresh-reader capability is a portability-of-understanding test.

Fresh-reader validation != promotion authorization.

---

## 1. Candidate / Promoted Distinction

R6-M executes against the current candidate implementation.

The current candidate does not yet contain the final promoted-v41:

- initiation statement;
- canonical load.

Therefore R6-M MUST distinguish:

1. candidate implementation readability;
2. final promoted-repository entry-point completeness.

Candidate readability may be validated before promotion.

Promoted-entry completeness may not be claimed before the promoted v41 entry
surfaces exist.

No fake completion.

No fake retrieval.

No fake canonical surface.

---

## 2. Current Candidate State

The current candidate preserves historical:

- v39 initiation;
- v40 initiation;
- v39 canonical load;
- v40 canonical load.

Historical versions may remain for provenance.

Historical versions MUST NOT silently become the v41 fresh-reader entry point.

v40 knowledge is not a permitted dependency for native v41 operation.

---

## 3. README Boundary

README is a repository orientation surface.

README currently establishes:

- Zervan purpose;
- Git-governed repository state;
- historical-version provenance;
- Authority NONE;
- Human Gate ACTIVE;
- external runtime disabled;
- external action disabled;
- system population disallowed;
- controlled read-only posture;
- no fake retrieval;
- no authority promotion;
- no compression out.

README orientation != complete native-v41 initialization.

README alone is insufficient for final promoted fresh-reader completion.

---

## 4. Inventory Boundary

INVENTORY declares conceptual existence.

Inventory does not define:

- current authority;
- precedence;
- native-v41 load order;
- final initiation;
- final canonical load.

Inventory != initialization.

Inventory != operational decoder ring.

Fresh readers may use Inventory for discovery, not as substitute doctrine.

---

## 5. Candidate Native Surface

The candidate implementation SHALL be navigable from repository-contained
surfaces.

A fresh reader must be able to locate:

- repository orientation;
- conceptual inventory;
- contracts;
- schemas;
- validation tools;
- tests;
- Ring 6 vector registry;
- Ring 6 runner.

No prior conversation is required to discover those implementation surfaces.

---

## 6. Contract Self-Containment

Native-v41 candidate contracts SHALL preserve their own:

- purpose;
- scope;
- dependencies;
- definitions;
- state vocabulary;
- ownership boundaries;
- authority boundaries;
- failure conditions;
- locks.

A contract MUST NOT require the reader to know what the architect "meant" in a
prior conversation.

Repository text must carry operational meaning.

---

## 7. Terminology

Native-v41 terminology MUST be resolvable from repository-contained contracts.

A fresh reader may encounter specialized terms including:

- Room;
- Origin;
- Zone;
- Space;
- Orientation;
- Cartography;
- Stick;
- Passageway;
- Qualification;
- Occupancy;
- Formation;
- Landing;
- Reflight;
- Replay;
- PMC;
- CCR;
- MC;
- Raven;
- Human Gate.

Specialized vocabulary is permitted.

Unstated private interpretation is not.

Personality != hidden dependency.

---

## 8. No v40 Dependency

A promoted native-v41 repository MUST NOT require a reader to load v40 first in
order to understand v41.

Historical v40 material may remain for provenance.

Historical preservation != current dependency.

v40 provenance != v41 initialization requirement.

---

## 9. No Conversation Dependency

Native-v41 operation MUST NOT require access to:

- this development conversation;
- prior ChatGPT state;
- stored memory;
- private scratchpad;
- undocumented verbal corrections.

Conversation may explain development history.

Conversation MUST NOT be required to operate the promoted repository.

Conversation != canonical dependency.

---

## 10. No Architect Dependency

The original architect MUST NOT be required as a live interpreter of ordinary
native-v41 operation.

Questions may still exist.

Evolution may still require Human Gate judgment.

But basic meaning, routing, boundaries, and validation must be represented in
the repository.

Architect availability != runtime prerequisite.

---

## 11. No Emotional-History Dependency

The process through which a rule was discovered may be preserved as history.

A fresh reader does not need to reproduce:

- frustration;
- argument;
- discovery cadence;
- jokes;
- emotional context

to understand the resulting invariant.

Discovery history != operational prerequisite.

---

## 12. Navigation Without Decoder Ring

The repository must expose enough structure for a fresh reader to answer:

1. What is Zervan?
2. What authority does it have?
3. What is the Human Gate state?
4. What is the current implementation identity?
5. Where are contracts?
6. Where are schemas?
7. Where are validators?
8. Where are tests?
9. How is validation run?
10. What is candidate versus promoted state?

If these require external explanation, fresh-reader portability is incomplete.

---

## 13. Candidate Validation Scope

R6-M candidate validation verifies:

- README orientation exists;
- Inventory exists;
- contracts directory exists;
- schemas directory exists;
- tools directory exists;
- tests directory exists;
- R6-A registry exists;
- Ring 6 runner exists;
- candidate validation contracts identify vTemporal.41.0;
- Authority NONE remains explicit;
- Human Gate ACTIVE remains explicit;
- candidate and promoted state are not conflated.

---

## 14. Promoted Entry Surfaces

Final promoted fresh-reader completion requires repository-contained v41 entry
surfaces including:

- native-v41 initiation statement;
- native-v41 canonical load or successor canonical entry artifact;
- explicit load / entry relationship;
- current-version identity;
- operating posture;
- authority boundary;
- Human Gate state.

Those surfaces do not currently exist in candidate `call/` and `canonical/`.

R6-M records that fact.

R6-M does not fabricate them.

---

## 15. Promotion Packaging Boundary

The absent final v41 entry surfaces are a promotion-packaging dependency.

They are not silently treated as nonexistent requirements.

They are not silently satisfied by v40 artifacts.

They are not created by the Fresh-Reader validator.

Validation observes.

Validation does not repair implementation state silently.

---

## 16. Candidate Outcome

R6-M defines candidate outcome:

`CANDIDATE_READABLE`

when the native candidate implementation is repository-contained and navigable
without external conversational context.

`BLOCKED`

when the candidate itself requires external interpretation to understand its
implementation surfaces.

CANDIDATE_READABLE != PROMOTED_FRESH_READER_COMPLETE.

---

## 17. Promoted Entry Outcome

R6-M separately records:

- `READY`;
- `DEFERRED_TO_PROMOTION`;
- `BLOCKED`.

For the present candidate branch:

`DEFERRED_TO_PROMOTION`

is correct when final v41 initiation / canonical entry artifacts do not yet
exist.

DEFERRED_TO_PROMOTION != failure.

DEFERRED_TO_PROMOTION != completion.

---

## 18. No Historical Substitution

The test MUST reject any claim that:

- v40 initiation == v41 initiation;
- v40 canonical load == v41 canonical load;
- historical presence == current authority;
- old version knowledge == current operating requirement.

Historical artifact != active v41 entry surface.

---

## 19. No Validator-Created Documentation

The Fresh-Reader validator MUST NOT modify README, initiation, canonical, or
doctrine surfaces merely to make the test pass.

Validator != documentation author.

Validator != promotion tool.

Validator != canonical mutation.

---

## 20. Portability of Understanding

Fresh-reader capability tests whether operational understanding travels with the
repository.

Understanding should survive transfer to:

- another machine;
- another session;
- another operator;
- another model;
- another reviewer.

Portable repository != shared memory.

---

## 21. Machine-Readable Record

Every R6-M validation record SHALL preserve:

- `validation_vector`;
- `repository_orientation_present`;
- `inventory_present`;
- `contracts_present`;
- `schemas_present`;
- `tools_present`;
- `tests_present`;
- `r6_registry_present`;
- `ring6_runner_present`;
- `candidate_version_identity_present`;
- `authority_none_present`;
- `human_gate_active_present`;
- `requires_v40_knowledge`;
- `requires_prior_conversation`;
- `requires_architect_interpretation`;
- `requires_emotional_discovery_history`;
- `v41_initiation_present`;
- `v41_canonical_entry_present`;
- `historical_v40_initiation_present`;
- `historical_v40_canonical_present`;
- `historical_substitution_used`;
- `candidate_readability`;
- `promoted_entry_state`;
- `promotion_dependency_references`;
- `provenance_route`;
- `authority_state`;
- `human_gate_state`;
- `validation_disposition`;
- `failure_reasons`.

---

## 22. Validation Disposition

R6-M defines:

- `VALIDATED_CANDIDATE`;
- `BLOCKED`.

VALIDATED_CANDIDATE means the candidate implementation is self-contained enough
for repository-only review and operation of its implemented candidate surfaces.

It does not claim final promoted-v41 initialization completeness.

BLOCKED means candidate readability itself failed.

---

## 23. Failure Reasons

R6-M recognizes:

- `REPOSITORY_ORIENTATION_MISSING`;
- `INVENTORY_MISSING`;
- `CONTRACT_SURFACE_MISSING`;
- `SCHEMA_SURFACE_MISSING`;
- `TOOL_SURFACE_MISSING`;
- `TEST_SURFACE_MISSING`;
- `R6_REGISTRY_MISSING`;
- `RING6_RUNNER_MISSING`;
- `CANDIDATE_VERSION_IDENTITY_MISSING`;
- `AUTHORITY_BOUNDARY_MISSING`;
- `HUMAN_GATE_BOUNDARY_MISSING`;
- `V40_KNOWLEDGE_REQUIRED`;
- `PRIOR_CONVERSATION_REQUIRED`;
- `ARCHITECT_INTERPRETATION_REQUIRED`;
- `EMOTIONAL_HISTORY_REQUIRED`;
- `HISTORICAL_VERSION_SUBSTITUTED`;
- `PROMOTED_ENTRY_FALSELY_CLAIMED`;
- `AUTHORITY_PROMOTED`.

Failure reasons accumulate.

No Compression Out applies.

---

## 24. Positive Controls

R6-M SHALL prove:

1. repository orientation is present;
2. conceptual inventory is present;
3. candidate contracts are discoverable;
4. candidate schemas are discoverable;
5. validation tools are discoverable;
6. tests are discoverable;
7. R6-A registry is discoverable;
8. Ring 6 runner is discoverable;
9. candidate v41 identity is represented;
10. Authority NONE is represented;
11. Human Gate ACTIVE is represented;
12. candidate operation does not require v40 knowledge;
13. candidate operation does not require prior conversation;
14. candidate operation does not require live architect interpretation;
15. final promoted entry-point incompleteness remains explicitly deferred rather
    than hidden.

---

## 25. Negative Controls

R6-M SHALL reject:

1. v40 treated as required v41 initialization;
2. prior conversation treated as required context;
3. architect oral interpretation treated as required context;
4. emotional discovery history treated as required context;
5. absent v41 initiation falsely claimed present;
6. absent v41 canonical entry falsely claimed present;
7. historical artifact silently substituted as active v41 entry;
8. authority promotion.

---

## 26. Promotion Completion Condition

Final promoted fresh-reader completion is not emitted by R6-M on the candidate
branch.

At promotion time, the fresh-reader condition becomes complete only when:

- v41 entry surfaces exist;
- those surfaces select native v41 directly;
- historical versions remain historical;
- repository-only initialization succeeds;
- no external decoder ring is required.

R6-M preserves this as a promotion dependency.

---

## 27. R6-M Lock

R6-M = Fresh-Reader Validation.

A fresh reader must not require v40 knowledge.

A fresh reader must not require prior conversation state.

A fresh reader must not require original architect interpretation.

A fresh reader must not require emotional discovery history.

A repository that requires an external decoder ring is incomplete.

Fresh-reader capability is a portability-of-understanding test.

Fresh-reader validation != promotion authorization.

Historical versions may remain for provenance.

Historical preservation != current dependency.

v40 provenance != v41 initialization requirement.

Conversation != canonical dependency.

Architect availability != runtime prerequisite.

Discovery history != operational prerequisite.

README orientation != complete native-v41 initialization.

Inventory != initialization.

Candidate readability may be validated before promotion.

Promoted-entry completeness may not be claimed before the promoted v41 entry
surfaces exist.

No fake completion.

No fake canonical surface.

Historical artifact != active v41 entry surface.

Validation observes.

Validation does not repair implementation state silently.

CANDIDATE_READABLE != PROMOTED_FRESH_READER_COMPLETE.

DEFERRED_TO_PROMOTION != failure.

DEFERRED_TO_PROMOTION != completion.

Validator != documentation author.

Validator != promotion tool.

Validator != canonical mutation.

Portable repository != shared memory.

Authority remains NONE.

Human Gate remains ACTIVE.

R6-N owns Representation Independence validation.
