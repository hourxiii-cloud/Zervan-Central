# R6-L — RBT-001 Recursive Butt Topology Validation

Status: CONTROLLED CANDIDATE VALIDATION CONTRACT
Ring: R6-L
Tranche: 8 — Validation and Audit
Validation Family: PERTURBATION
Validation Vector: RBT_001
Carrier: RBT-001 — Recursive Butt Topology
Version: vTemporal.41.0
Implementation Identity: v41 Complete
Promotion State: CANDIDATE
Authority: NONE
Human Gate: ACTIVE

---

## 0. Purpose

R6-L implements:

`RBT-001 — Recursive Butt Topology`

as the required memorable native-v41 regression carrier.

RBT-001 exercises:

- object isolation;
- signal differentiation;
- boundary preservation;
- Landing;
- Reflight;
- return without reconstruction.

Humor does not reduce test rigor.

Memorable carrier != reduced invariant coverage.

---

## 1. Carrier Boundary

`Recursive Butt Topology` is the name of the regression carrier.

The carrier does not create a new Zervan primitive.

The carrier vocabulary does not redefine:

- Room;
- Zone;
- Space;
- Territory;
- Cartography;
- Stick;
- Landing;
- Reflight;
- signal;
- evidence;
- provenance.

Carrier metaphor != doctrine.

Carrier label != semantic ownership.

---

## 2. Object Isolation

RBT-001 operates against one canonical test Room Object.

A separately declared control object remains independent.

The primary object MUST NOT:

- inherit control-object state;
- adopt the control-object identity;
- absorb control-object provenance;
- merge boundaries with the control object.

Object isolation != object ignorance.

Related test data != shared identity.

---

## 3. Primary Object

The primary test Room preserves:

- canonical `object_id`;
- Origin reference;
- Cartography reference;
- Stick reference;
- evidence boundary;
- evidence ceiling;
- provenance route.

The primary `object_id` remains stable through:

signal encounter
-> Landing
-> Reflight eligibility
-> return.

---

## 4. Control Object

RBT-001 SHALL preserve a distinct control object.

`primary_object_id != control_object_id`

The control object exists to prove isolation.

It is not a second perspective of the primary Room.

It is not silently merged into the primary Room.

Control object != alternate view.

---

## 5. Signal Differentiation

RBT-001 SHALL present at least two attributable signals.

The signals MUST remain separately identifiable.

Signal A != Signal B.

Different signal route != different Room by itself.

Signal differentiation MUST preserve:

- signal identity;
- signal source;
- signal route;
- evidence references;
- unresolved status where applicable.

---

## 6. No Signal Collapse

The test MUST reject silent collapse of distinguishable signals into one
unattributed signal.

Two signals do not become one merely because they touch the same Room.

Correlation != identity.

Similarity != identity.

Signal differentiation != forced disagreement.

---

## 7. Boundary Preservation

The primary Room evidence boundary before the carrier operation MUST equal the
primary Room evidence boundary after return unless a separately governed
boundary change exists.

RBT-001 positive controls use no boundary change.

The control object's boundary MUST NOT become the primary object's boundary.

Signal contact does not broaden evidence access.

---

## 8. Evidence Ceiling Preservation

The primary evidence ceiling remains unchanged throughout the positive carrier.

Signal presence does not raise claim authority.

Landing does not raise claim authority.

Reflight eligibility does not raise claim authority.

Return does not raise claim authority.

---

## 9. Landing

RBT-001 SHALL create or bind an attributable Landing Witness.

Landing preserves:

- canonical object identity;
- Orientation;
- representation position;
- Cartography;
- provenance;
- prior question;
- unresolved terrain;
- readiness.

Landing means movement stopped.

Landing != exit.

Landing != closure.

Landing != replay.

---

## 10. Landing Coordinate

The carrier preserves an exact landed coordinate.

The landed coordinate becomes the preserved source position for Reflight.

Landing coordinate MUST NOT be inferred later from memory.

Landing coordinate MUST NOT be reconstructed approximately.

---

## 11. Landing Object Identity

The Landing Witness MUST resolve to the primary Room Object.

`landing_object_id == primary_object_id`

Landing MUST NOT:

- clone the Room;
- create a snapshot Room;
- substitute the control object.

The Room remains the Room.

---

## 12. Reflight Trigger

RBT-001 SHALL bind a native Reflight Trigger.

The positive carrier uses an attributable changed analytical condition.

The trigger MUST preserve:

- Landing Witness reference;
- trigger type;
- trigger evidence;
- material-change basis;
- prior signal state;
- current signal state where applicable;
- evidence boundary;
- evidence ceiling;
- Cartography;
- Stick;
- readiness;
- provenance.

Trigger != execution.

---

## 13. Real Trigger Required

Reflight requires a real trigger.

RBT-001 MUST reject pseudo-triggers including:

- acknowledgment;
- repetition;
- celebration;
- affect;
- conversational continuation;
- capability availability alone.

No material change = no Reflight.

---

## 14. Signal Trigger

RBT-001 may exercise the native `SIGNAL_SPLIT` trigger because the carrier
already contains differentiated signal state.

When `SIGNAL_SPLIT` is used:

- prior signal-state reference is required;
- current signal-state reference is required;
- material-change basis is required.

Signal split != confidence.

Signal != trigger.

Trigger first.

Notify second.

---

## 15. Reflight Eligibility

RBT-001 preserves:

- `ELIGIBLE`;
- `BLOCKED`.

ELIGIBLE means the trigger and attributable basis support consideration of
renewed movement.

ELIGIBLE != executed.

ELIGIBLE != authority.

BLOCKED != nonexistent.

---

## 16. Reflight From Landing

Reflight MUST begin from preserved Landing state.

Landing first.

Trigger second.

Reflight MUST NOT infer the prior Room from carrier narrative.

Reflight MUST NOT rebuild an approximate Room.

Reflight != new Room.

---

## 17. Return

The carrier SHALL return to the preserved primary analytical position.

The returned object identity MUST equal the original primary object identity.

`returned_object_id == primary_object_id`

The returned coordinate MUST equal the attributable return coordinate required
by the carrier.

Return != reconstruction.

Return != replacement.

Return != new Genesis.

---

## 18. Return Without Reconstruction

The positive carrier requires:

`reconstruction_used = false`

RBT-001 MUST reject:

- approximate Room rebuilding;
- replacement object creation;
- synthetic return coordinate;
- reconstructed provenance;
- reconstructed Stick.

Return continuity must derive from preserved state.

---

## 19. Stick

RBT-001 MUST preserve Stick continuity through:

- signal differentiation;
- Landing;
- Reflight trigger;
- return.

Stick binds the carrier back to:

- object identity;
- Origin;
- Cartography;
- provenance;
- transform history;
- evidence lineage;
- return coordinates.

The memorable carrier does not weaken Stick requirements.

---

## 20. Provenance

Every meaningful RBT-001 event remains attributable.

The provenance route SHALL include:

- primary object;
- signal A;
- signal B;
- Landing;
- Reflight Trigger;
- return.

Carrier humor MUST NOT replace provenance.

Joke != provenance.

---

## 21. Signal Provenance

Each signal preserves its own provenance.

Signal A provenance MUST NOT silently become Signal B provenance.

Signal B provenance MUST NOT silently become Signal A provenance.

Shared downstream handling does not erase distinct signal origin.

---

## 22. Control-Object Provenance

Control-object provenance remains separate from primary-object provenance.

The carrier MUST reject provenance contamination where control-object-exclusive
material silently appears as primary-object provenance.

---

## 23. Boundary Contamination

RBT-001 MUST reject:

- primary boundary replaced by control boundary;
- primary boundary widened by signal availability;
- primary boundary widened merely because Reflight becomes ELIGIBLE.

Trigger != boundary expansion.

---

## 24. No Lifecycle Invention

RBT-001 validates carrier continuity.

It does not independently mutate lifecycle state.

Landing != lifecycle closure.

Reflight Trigger != lifecycle mutation.

Return != lifecycle promotion.

---

## 25. No Occupancy Invention

RBT-001 does not manufacture Occupancy merely from:

- signal existence;
- Landing;
- Reflight eligibility;
- return validation.

Occupancy remains separately witnessed.

Carrier != occupant.

---

## 26. No Formation Invention

Multiple differentiated signals do not automatically create:

- Stack Analysis;
- Expanded Analysis;
- Swarm.

Signal differentiation != formation selection.

RBT-001 does not silently escalate force or formation.

---

## 27. No Full Hydration

Reflight inside RBT-001 does not authorize full-payload hydration.

Payload rests.

Any hydration remains mission-required.

Carrier completeness != full payload requirement.

---

## 28. No Authority Promotion

The successful memorable carrier does not create:

- truth authority;
- execution authority;
- publication authority;
- canonical promotion authority.

Test success != truth.

Validation != authority.

Authority remains NONE.

Human Gate remains ACTIVE.

---

## 29. Validation Record

Every R6-L validation record SHALL preserve:

- `validation_vector`;
- `carrier_id`;
- `carrier_name`;
- `primary_object_id`;
- `control_object_id`;
- `primary_origin_reference`;
- `primary_cartography_reference`;
- `primary_stick_reference`;
- `primary_boundary_before`;
- `primary_boundary_after`;
- `control_boundary_reference`;
- `primary_evidence_ceiling_before`;
- `primary_evidence_ceiling_after`;
- `signals`;
- `landing_witness_reference`;
- `landing_object_id`;
- `landing_coordinate`;
- `reflight_trigger_type`;
- `reflight_trigger_evidence`;
- `reflight_material_change_basis`;
- `prior_signal_state_reference`;
- `current_signal_state_reference`;
- `reflight_eligibility`;
- `returned_object_id`;
- `return_coordinate`;
- `returned_coordinate`;
- `reconstruction_used`;
- `control_state_absorbed`;
- `control_provenance_absorbed`;
- `lifecycle_mutated`;
- `occupancy_invented`;
- `formation_invented`;
- `full_payload_hydration_requested`;
- `provenance_route`;
- `authority_state`;
- `human_gate_state`;
- `validation_disposition`;
- `failure_reasons`.

---

## 30. Signal Record

Each carrier signal SHALL preserve:

- `signal_id`;
- `signal_source`;
- `signal_route`;
- `evidence_references`;
- `provenance_route`.

Signal records remain independently attributable.

---

## 31. Validation Disposition

R6-L defines:

- `VALID`;
- `BLOCKED`.

VALID means RBT-001 completed its regression obligations without violating the
underlying native contracts.

BLOCKED means one or more carrier invariants failed.

VALID != truth.

VALID != authority.

---

## 32. Failure Reasons

R6-L recognizes:

- `OBJECT_ISOLATION_FAILED`;
- `CONTROL_OBJECT_IDENTITY_COLLISION`;
- `SIGNAL_DIFFERENTIATION_LOST`;
- `SIGNAL_PROVENANCE_LOST`;
- `PRIMARY_BOUNDARY_CHANGED`;
- `CONTROL_BOUNDARY_ABSORBED`;
- `EVIDENCE_CEILING_CHANGED`;
- `LANDING_WITNESS_MISSING`;
- `LANDING_OBJECT_IDENTITY_CHANGED`;
- `LANDING_COORDINATE_MISSING`;
- `REFLIGHT_TRIGGER_INVALID`;
- `REFLIGHT_TRIGGER_EVIDENCE_MISSING`;
- `REFLIGHT_MATERIAL_CHANGE_MISSING`;
- `SIGNAL_STATE_REFERENCE_MISSING`;
- `RETURN_OBJECT_IDENTITY_CHANGED`;
- `RETURN_COORDINATE_CHANGED`;
- `RECONSTRUCTION_USED`;
- `STICK_NOT_PRESERVED`;
- `CONTROL_PROVENANCE_ABSORBED`;
- `LIFECYCLE_MUTATED`;
- `OCCUPANCY_INVENTED`;
- `FORMATION_INVENTED`;
- `FULL_PAYLOAD_HYDRATION_REQUESTED`;
- `AUTHORITY_PROMOTED`.

Failure reasons accumulate.

No Compression Out applies.

---

## 33. Positive Control

R6-L SHALL prove this carrier route:

Primary Room
-> Signal A + Signal B remain distinguishable
-> primary boundary remains stable
-> Landing at exact coordinate
-> attributable SIGNAL_SPLIT trigger
-> Reflight ELIGIBLE
-> return to original primary Room
-> exact return coordinate
-> no reconstruction

while the control object remains isolated.

---

## 34. Negative Controls

R6-L SHALL reject:

1. primary/control object identity collision;
2. control state absorbed into primary object;
3. signals silently collapsed into one;
4. signal provenance lost;
5. primary boundary change;
6. control boundary imported into primary;
7. evidence-ceiling elevation;
8. Landing without witness;
9. Landing against wrong object;
10. Landing without coordinate;
11. invalid Reflight trigger;
12. SIGNAL_SPLIT without signal-state references;
13. trigger without material-change basis;
14. return to different object;
15. changed return coordinate;
16. reconstruction;
17. missing Stick;
18. control provenance contamination;
19. lifecycle mutation;
20. invented Occupancy;
21. invented Formation;
22. full-payload hydration;
23. authority promotion.

---

## 35. R6-L Lock

R6-L = RBT-001 — Recursive Butt Topology.

RBT-001 is a memorable regression carrier.

RBT-001 exercises object isolation.

RBT-001 exercises signal differentiation.

RBT-001 exercises boundary preservation.

RBT-001 exercises Landing.

RBT-001 exercises Reflight.

RBT-001 exercises return without reconstruction.

Humor does not reduce test rigor.

Memorable carrier != reduced invariant coverage.

Carrier metaphor != doctrine.

Carrier label != semantic ownership.

primary_object_id != control_object_id.

Control object != alternate view.

Signal A != Signal B.

Different signal route != different Room by itself.

Correlation != identity.

Similarity != identity.

Signal contact does not broaden evidence access.

Signal presence does not raise claim authority.

Landing means movement stopped.

Landing != exit.

Landing != closure.

Landing != replay.

Landing coordinate MUST NOT be reconstructed approximately.

landing_object_id == primary_object_id.

The Room remains the Room.

Trigger != execution.

Reflight requires a real trigger.

No material change = no Reflight.

Signal split != confidence.

Signal != trigger.

Landing first.

Trigger second.

Reflight MUST NOT rebuild an approximate Room.

Reflight != new Room.

returned_object_id == primary_object_id.

Return != reconstruction.

Return != replacement.

Return != new Genesis.

The memorable carrier does not weaken Stick requirements.

Joke != provenance.

Trigger != boundary expansion.

Signal differentiation != formation selection.

Payload rests.

Carrier completeness != full payload requirement.

Test success != truth.

Validation != authority.

Authority remains NONE.

Human Gate remains ACTIVE.

R6-M owns Fresh-Reader validation.
