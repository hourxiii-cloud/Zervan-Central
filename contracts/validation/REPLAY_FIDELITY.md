# R6-I — Replay Fidelity Validation

Status: CONTROLLED CANDIDATE VALIDATION CONTRACT
Ring: R6-I
Tranche: 8 — Validation and Audit
Validation Family: REPLAY
Validation Vector: REPLAY_FIDELITY
Version: vTemporal.41.0
Implementation Identity: v41 Complete
Promotion State: CANDIDATE
Authority: NONE
Human Gate: ACTIVE

---

## 0. Purpose

R6-I implements the native-v41 Replay Fidelity vector.

Validation SHALL verify that a historical Room coordinate may be reopened while
preserving:

- object identity;
- state lineage;
- transform history;
- evidence route.

Replay != reconstruction.

Replay != new Room.

---

## 1. Replay Principle

Replay is the reopening of the same Room at a preserved observational
coordinate.

Replay reopens.

Replay does not reconstruct an approximation.

Same history revisited != new world.

Historical coordinate != alternate reality.

---

## 2. Required Historical Surface

Replay fidelity requires preservation of:

- canonical Room Object identity;
- Closing Witness reference;
- preserved lineage;
- historical question contract;
- historical inquiry envelope;
- departure coordinate;
- replay observational coordinate;
- occupied Zone;
- occupied Space;
- transform history;
- evidence used;
- capability movement;
- evidence boundary;
- evidence ceiling;
- Restriction / Constriction history;
- Collapse history;
- findings;
- rendering state;
- return coordinate;
- Cartography;
- Stick;
- provenance.

No required historical element may silently be replaced by a summary.

---

## 3. Same Object

Replay MUST preserve canonical `object_id`.

Replay MUST NOT:

- mint a replacement Room;
- mint a snapshot Room;
- infer another object from similar content;
- treat historical state as another world.

Replay != new Room.

---

## 4. Preserved Coordinate

Replay MUST bind the exact historical observational coordinate.

An approximate remembered coordinate is insufficient.

A prose description of the prior location is insufficient.

A reconstructed coordinate is insufficient.

Preserved coordinate != reconstructed coordinate.

---

## 5. Historical Question

Replay preserves the historical question contract.

The current observer's question MUST NOT overwrite the historical question.

Historical question != current question.

A new question may be issued separately after replay.

---

## 6. Historical Inquiry Envelope

Replay preserves the inquiry envelope that bounded the historical operation.

Current context MUST NOT retroactively widen historical scope.

Historical scope remains historical scope.

Available now != in scope then.

---

## 7. Representation Fidelity

Replay preserves:

- historical Zone;
- historical Space;
- transform history;
- Cartography;
- observational coordinate.

Current defaults MUST NOT replace historical representation state.

No default substitution.

Transform history MUST NOT be flattened.

Replay fidelity requires geometry fidelity.

---

## 8. Evidence-Use Fidelity

Replay preserves evidence actually used in the historical operation.

Current evidence availability MUST NOT rewrite historical evidence use.

Evidence available now != evidence used then.

Excluded then != included then merely because it is available now.

Historical evidence route remains historical evidence route.

---

## 9. Evidence Boundary

Replay preserves the historical evidence boundary.

Replay MUST NOT retroactively expose excluded material.

Historical boundary remains attributable.

Replay != evidence-boundary expansion.

---

## 10. Evidence Ceiling

Replay preserves the historical evidence ceiling.

Later evidence or a later claim ceiling MUST NOT silently strengthen historical
findings.

Replay fidelity != retrospective promotion.

Later authority != historical authority.

---

## 11. Capability Movement

Replay preserves historical capability movement.

The movement route MUST NOT be replaced by the final finding alone.

Capability route is part of analytical history.

Finding-only replay is insufficient.

---

## 12. Restriction Fidelity

Replay preserves Restriction / Constriction history.

Previously restricted material MUST NOT silently become:

- included;
- absent;
- nonexistent.

Restriction history remains evidence.

Restriction != absence.

---

## 13. Collapse Fidelity

Replay preserves Collapse Boundaries.

Replay MUST retain attributable:

- collapse trigger;
- governing evidence;
- removed possibilities;
- retained possibilities;
- deferred possibilities;
- surviving uncertainty;
- newly available frontier;
- route back.

Replay does not flatten Collapse into the eventual answer.

---

## 14. Finding Fidelity

Replay preserves historical findings as historical findings.

Finding != truth authority.

Historical finding != current interpretation.

Historical finding != current publication authority.

---

## 15. Rendering Fidelity

Where a historical rendering exists, Replay preserves its reference.

Where no historical rendering exists, Replay MUST preserve that absence.

A regenerated rendering MUST NOT be represented as the historical original.

Rendering != Room Object.

Rendering != evidence.

---

## 16. Return Coordinate

Replay preserves the historical return coordinate.

Return coordinate preserves analytical continuity.

Return coordinate != rollback authority.

Return coordinate MUST NOT be fabricated.

---

## 17. Landing Relationship

Landing preserves the state from which Replay may later operate.

Landing preserves:

- Room identity;
- Orientation;
- representation position;
- Cartography;
- provenance;
- prior question;
- prior rendering;
- unresolved terrain;
- readiness;
- return coordinates.

Replay consumes preserved Landing state.

Replay MUST NOT use Landing as an excuse to reconstruct missing history.

Landing != Replay.

---

## 18. Stick

Replay MUST preserve Stick continuity.

The replayed coordinate remains connected to:

- canonical object identity;
- Origin;
- lineage;
- question;
- representation;
- transform history;
- evidence;
- capability movement;
- findings;
- return route.

Replay MUST NOT sever analytical continuity.

BROKEN continuity fails closed.

---

## 19. Hydration Boundary

Replay does not justify full reconstruction or full hydration.

Only mission-required verified territory may be hydrated.

Payload rests.

Replay fidelity != full payload reload.

---

## 20. Replay Eligibility

Replay preserves native eligibility states:

- `ELIGIBLE`;
- `BLOCKED`.

ELIGIBLE means preserved state is sufficiently attributable for reopening
consideration.

ELIGIBLE != reopened.

ELIGIBLE != execution authority.

BLOCKED != nonexistent historical state.

---

## 21. Fail Closed

If required preserved state is insufficient, Replay is BLOCKED.

The system MUST NOT invent:

- identity;
- lineage;
- coordinate;
- question;
- inquiry envelope;
- transform history;
- evidence history;
- movement;
- restriction history;
- Collapse history;
- findings;
- rendering;
- return route;
- provenance.

Fail closed.

Do not fabricate history.

---

## 22. No Retrospective Substitution

R6-I MUST reject substitution of:

- current question for historical question;
- current Zone for historical Zone;
- current Space for historical Space;
- current evidence for historical evidence;
- current claim ceiling for historical claim ceiling;
- current rendering for missing historical rendering;
- current coordinates for preserved historical coordinates.

Current convenience != replay fidelity.

---

## 23. No New World

Replay MUST NOT create:

- new Room identity;
- Branch by default;
- independent Genesis;
- synthesized replacement object.

Replay Envelope != Branch.

Replay does not create a duplicate world.

Observation history accumulates on one object.

---

## 24. Lifecycle Boundary

Replay validation does not itself transition:

`SEALED -> REOPENED`

Replay Envelope != lifecycle transition.

Eligibility != Registry mutation.

---

## 25. Reflight Boundary

Replay != Reflight Trigger.

Replay != Reflight execution.

Replay may support later continuation.

Replay does not itself restart analytical movement.

---

## 26. Publication Boundary

Replay does not alter:

Evidence
-> PMC
-> CCR
-> MC
-> Raven
-> Human Gate

Replay Envelope != publication record.

Historical finding != current publication authorization.

---

## 27. Validation Record

Every R6-I validation record SHALL preserve:

- `validation_vector`;
- `historical_object_id`;
- `replay_object_id`;
- `closing_witness_reference`;
- `historical_lineage_reference`;
- `replay_lineage_reference`;
- `historical_question_reference`;
- `replay_question_reference`;
- `historical_inquiry_envelope_reference`;
- `replay_inquiry_envelope_reference`;
- `historical_departure_coordinate`;
- `historical_observational_coordinate`;
- `replay_observational_coordinate`;
- `historical_zone_reference`;
- `replay_zone_reference`;
- `historical_space_reference`;
- `replay_space_reference`;
- `historical_transform_references`;
- `replay_transform_references`;
- `historical_evidence_used_references`;
- `replay_evidence_used_references`;
- `historical_capability_movement_references`;
- `replay_capability_movement_references`;
- `historical_evidence_boundary_reference`;
- `replay_evidence_boundary_reference`;
- `historical_evidence_ceiling_reference`;
- `replay_evidence_ceiling_reference`;
- `historical_restriction_references`;
- `replay_restriction_references`;
- `historical_collapse_references`;
- `replay_collapse_references`;
- `historical_finding_references`;
- `replay_finding_references`;
- `historical_rendering_reference`;
- `replay_rendering_reference`;
- `historical_return_coordinate`;
- `replay_return_coordinate`;
- `historical_cartography_reference`;
- `replay_cartography_reference`;
- `historical_stick_reference`;
- `replay_stick_reference`;
- `approximate_reconstruction_used`;
- `new_room_created`;
- `branch_created`;
- `lifecycle_mutated`;
- `reflight_executed`;
- `full_payload_hydration_requested`;
- `replay_eligibility`;
- `provenance_route`;
- `authority_state`;
- `human_gate_state`;
- `validation_disposition`;
- `failure_reasons`.

---

## 28. Validation Disposition

R6-I defines:

- `VALID`;
- `BLOCKED`.

VALID means replay preserves the required historical coordinate and lineage
without prohibited substitution.

BLOCKED means replay fidelity is insufficient.

VALID != reopened.

VALID != authority.

---

## 29. Failure Reasons

R6-I recognizes:

- `OBJECT_IDENTITY_CHANGED`;
- `CLOSING_WITNESS_MISSING`;
- `LINEAGE_CHANGED`;
- `HISTORICAL_QUESTION_SUBSTITUTED`;
- `INQUIRY_ENVELOPE_SUBSTITUTED`;
- `OBSERVATIONAL_COORDINATE_CHANGED`;
- `ZONE_SUBSTITUTED`;
- `SPACE_SUBSTITUTED`;
- `TRANSFORM_HISTORY_CHANGED`;
- `EVIDENCE_USED_CHANGED`;
- `CAPABILITY_MOVEMENT_CHANGED`;
- `EVIDENCE_BOUNDARY_CHANGED`;
- `EVIDENCE_CEILING_CHANGED`;
- `RESTRICTION_HISTORY_CHANGED`;
- `COLLAPSE_HISTORY_CHANGED`;
- `FINDING_HISTORY_CHANGED`;
- `HISTORICAL_RENDERING_SUBSTITUTED`;
- `RETURN_COORDINATE_CHANGED`;
- `CARTOGRAPHY_CHANGED`;
- `STICK_CHANGED`;
- `APPROXIMATE_RECONSTRUCTION_USED`;
- `NEW_ROOM_INVENTED`;
- `BRANCH_INVENTED`;
- `LIFECYCLE_MUTATED`;
- `REFLIGHT_EXECUTED`;
- `FULL_PAYLOAD_HYDRATION_REQUESTED`;
- `AUTHORITY_PROMOTED`.

Failure reasons accumulate.

No Compression Out applies.

---

## 30. Positive Control

R6-I SHALL prove that an exact preserved Room coordinate can be replayed while
maintaining identical:

- object identity;
- lineage;
- historical question;
- inquiry envelope;
- Zone;
- Space;
- transform history;
- evidence used;
- capability movement;
- evidence boundary;
- evidence ceiling;
- restriction history;
- Collapse history;
- findings;
- rendering state;
- return coordinate;
- Cartography;
- Stick.

---

## 31. Missing-Rendering Control

A historical rendering may legitimately be absent.

Replay MUST preserve:

`historical_rendering_reference = null`

as:

`replay_rendering_reference = null`

Replay MUST NOT synthesize a rendering and claim it was historical.

Missing != permission to fabricate.

---

## 32. Negative Controls

R6-I SHALL reject:

1. changed Room identity;
2. missing Closing Witness;
3. changed lineage;
4. substituted current question;
5. substituted current inquiry envelope;
6. reconstructed observational coordinate;
7. substituted Zone or Space;
8. flattened transform history;
9. changed historical evidence use;
10. changed capability route;
11. widened evidence boundary;
12. increased historical evidence ceiling;
13. erased restriction history;
14. erased Collapse history;
15. changed findings;
16. regenerated historical rendering represented as original;
17. changed return coordinate;
18. changed Cartography;
19. changed Stick;
20. approximate reconstruction;
21. new Room creation;
22. automatic Branch;
23. lifecycle mutation;
24. Reflight execution;
25. full-payload hydration;
26. authority promotion.

---

## 33. R6-I Lock

R6-I = Replay Fidelity.

Replay != reconstruction.

Replay != new Room.

Replay reopens the same Room at a preserved observational coordinate.

Same history revisited != new world.

Historical coordinate != alternate reality.

No required historical element may silently be replaced by a summary.

Preserved coordinate != reconstructed coordinate.

Historical question != current question.

Historical scope remains historical scope.

No default substitution.

Replay fidelity requires geometry fidelity.

Evidence available now != evidence used then.

Historical evidence route remains historical evidence route.

Replay != evidence-boundary expansion.

Replay fidelity != retrospective promotion.

Capability route is part of analytical history.

Restriction != absence.

Replay does not flatten Collapse into the eventual answer.

Finding != truth authority.

Historical finding != current interpretation.

Rendering != Room Object.

Rendering != evidence.

Return coordinate != rollback authority.

Landing != Replay.

Replay MUST preserve the Stick.

Replay MUST NOT sever analytical continuity.

Replay fidelity != full payload reload.

ELIGIBLE != reopened.

ELIGIBLE != execution authority.

BLOCKED != nonexistent historical state.

Fail closed.

Do not fabricate history.

Current convenience != replay fidelity.

Replay Envelope != Branch.

Replay does not create a duplicate world.

Observation history accumulates on one object.

Replay Envelope != lifecycle transition.

Replay != Reflight Trigger.

Replay != Reflight execution.

Replay Envelope != publication record.

VALID != reopened.

VALID != authority.

Missing != permission to fabricate.

Authority remains NONE.

Human Gate remains ACTIVE.

R6-J owns PMC / CCR / MC Compatibility validation.
