# Zervan v41 Operations Guide

Status: CONTROLLED CANDIDATE DOCUMENTATION
Ring: R7-H
Responsibility: CONSISTENT RUNTIME OPERATION
Version: vTemporal.41.0
Implementation Identity: v41 Complete
Stability Baseline: ae8dd9e22a34589f546f64ed452c0eee976cf299
Promotion State: CANDIDATE
Authority: NONE
Human Gate: ACTIVE

---

## 1. Purpose

This guide defines consistent operation of the stabilized native Zervan v41
candidate.

Operations applies implemented state.

Operations does not redefine architecture.

Operations does not replace implementation.

Operations does not perform audit ownership.

Operations does not grant promotion authority.

Current Git remains implementation truth.

---

## 2. Current Runtime Posture

The stabilized candidate operates under:

External Runtime: DISABLED

External Action: DISABLED

System Population: DISALLOWED

Canonical Mutation: HUMAN_GATE_ONLY

Authority: NONE

Human Gate: ACTIVE

Promotion State: CANDIDATE

These conditions are operating boundaries, not suggestions.

---

## 3. Startup Identity Check

Before operation, resolve:

1. `VERSION`
2. `VERSION.json`
3. `VERSION_AUTHORITY.md`
4. `call/INITIATION_STATEMENT_V41_0.md`
5. `canonical/ZERVAN_v41_0_CANONICAL_ENTRY.md`

Expected native identity:

`vTemporal.41.0`

Expected implementation identity:

`v41 Complete`

Expected candidate branch:

`candidate/v41-complete`

Do not operate from historical version assumptions.

---

## 4. Repository State Check

Before modifying or validating candidate state:

- confirm current branch;
- inspect `git status --short`;
- identify unrelated changes;
- preserve generated inventory drift separately;
- do not silently overwrite uncommitted work.

Repository state must be known before operation begins.

---

## 5. Operating Object

The Room is the primary analytical object.

Operate on the existing Room unless distinct-object evidence requires otherwise.

Perspective does not create another Room.

Zone does not create another Room.

Space does not create another Room.

Representation does not create another Room.

Replay does not create another Room.

---

## 6. Orientation

Before reasoning, determine operational position.

Preserve the native geography:

Origin
->
Ingress
->
Genesis
->
Room
->
Territory
->
Terrain
->
Zone
->
Space
->
Representation Transform
->
Cartography
->
Stick continuity

If position is unknown, establish orientation before analysis.

---

## 7. Qualification Gate

Qualification precedes normal analysis.

Operations must confirm the relevant Room and operation are sufficiently
qualified.

A READY request does not automatically mean the Room is READY.

A QUALIFIED Room does not automatically mean the requested operation is
permitted.

Blocked operation should remain blocked.

---

## 8. Occupancy

Record governed presence where occupancy applies.

Occupancy does not grant ownership.

Occupancy does not grant authority.

Exit does not destroy Room state.

Stale or revoked occupancy must not remain silently ACTIVE.

---

## 9. Question Intake

Use the governed Question Contract for recurring or reproducible inquiry.

Surface:

`contracts/question/QUESTION_CONTRACT.md`

Confirm:

- question;
- scope;
- time boundary;
- evidence boundary;
- evidence ceiling;
- exclusions;
- required artifacts;
- rendering requirements;
- provenance;
- Human Gate requirements.

Question execution remains read-only.

---

## 10. Evidence Boundary Check

Before analysis, confirm what evidence may participate.

Reachable evidence is not automatically admissible evidence.

Do not expand the boundary because additional data is available.

Boundary changes must be explicit.

---

## 11. Evidence Ceiling Check

Confirm the maximum supported claim strength before output.

Do not raise the evidence ceiling through:

- stronger language;
- personalization;
- audience selection;
- rendering;
- decision-option formatting.

Claim strength follows evidence.

---

## 12. Capability Routing

Use the smallest justified capability.

Need determines force.

Question determines mission.

Evidence determines escalation.

Do not escalate merely because a larger capability exists.

---

## 13. Formation Operation

Scale and Formation remain separate.

If Formation changes, preserve:

- trigger;
- justification;
- prior Formation;
- new Formation.

Do not silently escalate to a larger Formation.

---

## 14. Hydration

Hydrate only mission-required payload.

Identity may travel.

Payload rests until required.

Do not expose all reachable data simply because it can be fetched.

Hydration remains bounded by authorization, question, mission, evidence
boundary, and evidence ceiling.

---

## 15. Restriction and Constriction

Apply restrictions without converting hidden content into nonexistent content.

Restricted != nonexistent.

Constriction narrows the working surface.

Downstream operation must not silently widen it.

Preserve exclusions.

---

## 16. Perspective Rotation

When another perspective is needed, rotate the same object.

Preserve:

- Room identity;
- revision;
- lineage;
- provenance;
- Zone;
- Space;
- transform history;
- Cartography;
- evidence boundary;
- evidence ceiling.

Turn the object.

Do not clone the world.

---

## 17. Distinct Object Handling

Use the identity outcomes:

- SAME_OBJECT;
- DISTINCT_OBJECT;
- UNRESOLVED.

Do not force UNRESOLVED into sameness or distinction.

Create a distinct Room only when affirmative evidence supports it.

Use Passageways for governed cross-Room relationships.

---

## 18. Analysis Route

The native analytical route remains:

Evidence
->
PMC
->
CCR
->
MC
->
Raven
->
Human Gate

Operations must not bypass required stages.

Stage success does not grant downstream authority.

---

## 19. PMC Operation

PMC performs bounded analytical computation.

PMC must remain inside:

- qualified scope;
- evidence boundary;
- evidence ceiling;
- permitted operation.

PMC output is analytical state.

PMC output is not authority.

---

## 20. CCR Operation

CCR records candidate commitment.

Preserve:

- selected candidate;
- rejected candidates;
- null outcome;
- uncertainty;
- provenance;
- evidence boundary;
- evidence ceiling.

Do not bypass CCR when the governed pipeline requires it.

---

## 21. MC Operation

MC governs response and output admissibility.

MC does not replace PMC reasoning.

MC does not erase CCR history.

MC does not grant Human Gate authority.

---

## 22. Raven Operation

Raven renders governed analytical state.

Rendering may change presentation.

Rendering must not change:

- evidence;
- object identity;
- provenance;
- evidence boundary;
- evidence ceiling;
- decision lineage.

Report != Room.

---

## 23. Human Gate Operation

Human Gate remains the authority-bearing boundary.

Analytical success does not authorize execution.

Validation success does not authorize execution.

Receipt production does not authorize execution.

Approval != execution.

External action requires separately authorized operation.

---

## 24. Collapse Operation

Collapse may record supported convergence.

Preserve:

- Room identity;
- evidence;
- constraints;
- route;
- provenance;
- unresolved frontier.

Collapse != authority.

Collapse != publication.

Collapse != promotion.

Collapse != new Room.

---

## 25. Landing Operation

Landing preserves the arrival coordinate.

Preserve applicable:

- Room identity;
- revision;
- question;
- orientation;
- representation state;
- evidence boundary;
- evidence ceiling;
- unresolved conditions;
- provenance.

Landing must support later continuation.

---

## 26. Reflight Operation

Reflight requires a governed trigger.

Valid triggers may include:

- materially changed question;
- materially changed evidence;
- new permitted operation;
- other governed state change.

Do not automatically re-analyze simply because analysis can continue.

Preserve the Reflight reason.

---

## 27. Replay Operation

Replay returns to a historical observational coordinate.

Do not silently use present defaults.

Preserve historical:

- Room identity;
- question where applicable;
- boundaries;
- ceiling;
- exclusions;
- representation;
- provenance.

Replay does not recreate Genesis.

Replay does not mutate history.

---

## 28. Scar Operation

Preserve significant failure and unresolved conditions as Scar state where
governed.

Do not erase a Scar because a later operation succeeds.

Scar remains attributable.

Scar Replay should reproduce the relevant historical failure context where
supported.

---

## 29. Blocked Operation

BLOCKED is a valid result.

Do not manufacture success to avoid a blocked state.

When blocked, preserve:

- blocking condition;
- missing prerequisite;
- affected operation;
- evidence;
- next required condition where known.

Blocked != failed system integrity.

---

## 30. Provisional Operation

PROVISIONAL may preserve useful bounded state when full qualification or evidence
is incomplete.

Do not present provisional state as final.

Preserve the reason for provisional status.

Maintain evidence ceilings accordingly.

---

## 31. Degraded Operation

DEGRADED operation preserves what remains usable.

Record:

- lost capability;
- restricted capability;
- preserved capability;
- impact on claims;
- impact on evidence ceiling.

Degraded != invisible failure.

---

## 32. Rejected Operation

REJECTED is a governed outcome.

Preserve why the operation was rejected.

Do not silently reroute around a rejection when the rejection represents a
boundary.

Rejected routes remain provenance.

---

## 33. Unresolved Operation

UNRESOLVED is a valid analytical state.

Do not force resolution when evidence does not support it.

Preserve what additional evidence would discriminate where known.

Unknown != false.

Unknown != true.

---

## 34. Failure Classification

When operation fails validation, classify before repair:

- `INNER_INVARIANT_CONTRADICTION`
- `OUTER_IMPLEMENTATION_DEFECT`
- `VALIDATOR_DEFECT`
- `COVERAGE_GAP`
- `SOURCE_COLLISION`
- `UNRESOLVED_REQUIRED_SURFACE`

Repair the owning layer.

Do not repair across the wrong boundary.

---

## 35. Validator Failure Procedure

If a validator fails:

1. preserve the actual output;
2. identify the source it claims to validate;
3. verify the source semantics;
4. classify the failure;
5. repair the smallest correct surface;
6. rerun bounded validation.

Do not mutate correct implementation merely to obtain PASS.

---

## 36. Fast Gate

For a bounded subsection, use:

1. subsection validator;
2. subsection unit tests;
3. `git diff --check`;
4. `git status --short`.

Use the full aggregate only at designated closure points or for explicit
cross-section diagnosis.

Fast gate != final closure.

---

## 37. Commit Discipline

Commit only intended files.

Do not use broad staging when unrelated files are dirty.

Confirm staged whitespace:

`git diff --cached --check`

Inspect staged state before commit.

Push only after successful commit.

Do not claim completion that Git does not prove.

---

## 38. VERSION_REFERENCES.json

`VERSION_REFERENCES.json` is a generated reference inventory.

It is not active version authority.

Keep unrelated inventory drift out of bounded subsection commits.

Reconcile it deliberately at the proper completion stage.

---

## 39. Return to Existing Work

When returning to existing analytical work:

1. recover Room identity;
2. recover revision;
3. recover prior question;
4. recover Landing;
5. recover orientation;
6. recover evidence boundary;
7. recover evidence ceiling;
8. recover restrictions;
9. recover unresolved state;
10. recover provenance.

Then determine the correct continuation operation.

---

## 40. Runtime Restart

After a restart or new session, do not rebuild analytical state from memory.

Recover governed state from repository and recorded artifacts.

Where exact historical state cannot be reconstructed, preserve the gap.

Do not silently synthesize missing state.

---

## 41. Replacement Operation

If an implementation component is replaced, preserve its governed contract.

Replacement must preserve:

- interface;
- semantics;
- provenance;
- failure behavior;
- identity behavior;
- boundary behavior.

Replacement is not permission to redefine ownership.

---

## 42. Resilience Operation

Resilience means preserving governed behavior under stress and failure.

Correct outcomes may include:

- PASS;
- BLOCKED;
- DEGRADED;
- REJECTED;
- UNRESOLVED;
- SCARRED.

Resilience does not require fabricated success.

---

## 43. Provenance Operation

Meaningful state changes must remain attributable.

Preserve applicable:

- source;
- object identity;
- input identity;
- operation;
- governing contract;
- transform;
- output identity;
- state transition;
- failure;
- actor or capability.

Provenance != endorsement.

---

## 44. Documentation Use

Use documentation according to ownership:

README:

ORIENTATION

User Manual:

UNDERSTANDING AND OPERATION

Architecture Guide:

RATIONALE AND PRIMITIVES

Developer Guide:

IMPLEMENTATION AND EXTENSION

Audit Guide:

VERIFICATION / REPLACEMENT / RESILIENCE / PROVENANCE / COMPLETION CRITERIA

Operations Guide:

CONSISTENT RUNTIME OPERATION

Documentation does not replace implementation state.

---

## 45. Stability Boundary

R7-J established the documentation stability baseline.

Substantive implementation change after that baseline requires stability
revalidation.

Documentation clarification alone does not automatically invalidate
implementation stability.

Stability != completeness.

---

## 46. Transition Boundary

R7-I records transition accounting.

Transition accounting != promotion.

Operations must not interpret the transition receipt as authorization to mutate
main.

The future canonical transition remains separately Human-Gated.

---

## 47. Promotion Boundary

The candidate remains:

Promotion State: CANDIDATE

Ring 7 target:

READY_FOR_HUMAN_GATE

Operations does not perform canonical promotion.

Validation does not perform canonical promotion.

Documentation does not perform canonical promotion.

Receipt generation does not perform canonical promotion.

---

## 48. Historical State

Historical v39 and v40 artifacts may remain for provenance.

Do not operate from them as active native-v41 authority.

Historical artifact != active implementation.

Use active native-v41 version surfaces.

---

## 49. Normal Operational Sequence

A normal runtime sequence is:

1. resolve candidate identity;
2. inspect repository state;
3. identify or recover the Room;
4. establish orientation;
5. establish the question;
6. confirm evidence boundary;
7. confirm evidence ceiling;
8. confirm qualification;
9. confirm occupancy where applicable;
10. select proportional capability;
11. hydrate mission-required material;
12. operate inside the governed representation;
13. preserve provenance and uncertainty;
14. move through PMC;
15. preserve CCR;
16. apply MC admissibility;
17. render through Raven;
18. preserve Landing;
19. send authority-bearing decisions to Human Gate;
20. perform no unauthorized external action.

---

## 50. Operational Stop Conditions

Stop or block operation when:

- identity cannot be safely determined;
- required qualification is absent;
- evidence boundary is undefined where required;
- evidence ceiling is undefined where required;
- required provenance is missing;
- authority is insufficient;
- a source collision affects the operation;
- an invariant contradiction exists;
- required surface is unresolved.

Stopping correctly is valid operation.

---

## 51. Operational Recovery

After a failure:

1. preserve the failure;
2. preserve current state;
3. classify;
4. determine owner;
5. repair only if authorized and appropriate;
6. rerun the bounded gate;
7. preserve resulting provenance;
8. resume from a known coordinate.

Do not conceal the original failure.

---

## 52. No-Default Drift

Do not silently replace explicit state with generic defaults.

When a governed value exists, use it.

Use a default only when the governing implementation defines that default for an
otherwise undefined value.

Historical Replay must not silently inherit present defaults.

---

## 53. External Runtime Boundary

External Runtime remains DISABLED.

This guide does not enable it.

External integration capability does not imply external execution permission.

Any future runtime enablement requires its own governed change and authorization.

---

## 54. External Action Boundary

External Action remains DISABLED.

Analysis, reporting, recommendations, receipts, and approvals do not themselves
execute external actions.

Approval != execution.

Execution requires its own authorized path.

---

## 55. System Population Boundary

System Population remains DISALLOWED.

Do not silently populate external systems from candidate analytical state.

Output availability does not create population authority.

---

## 56. Canonical Mutation Boundary

Canonical Mutation remains HUMAN_GATE_ONLY.

Candidate work may prepare promotion evidence.

Candidate work may not silently mutate main.

READY_FOR_HUMAN_GATE is not CANONICAL.

---

## 57. Operations Closure

Native-v41 operations can consistently:

- resolve candidate identity;
- preserve one-object continuity;
- maintain operational geography;
- qualify before analysis;
- preserve occupancy boundaries;
- execute governed questions;
- enforce evidence boundaries and ceilings;
- route proportional capability;
- hydrate on mission need;
- preserve restrictions;
- rotate representations without cloning identity;
- preserve collapse, Landing, Reflight, Replay, and Scar;
- maintain the PMC / CCR / MC / Raven / Human Gate pipeline;
- classify failures;
- preserve provenance;
- respect runtime and promotion boundaries.

This guide defines consistent runtime operation.

It does not redefine architecture.

It does not absorb audit ownership.

It does not grant external action.

It does not perform promotion.

OPERATIONS GUIDE COMPLETE.

R7-K owns the Completeness Receipt.
